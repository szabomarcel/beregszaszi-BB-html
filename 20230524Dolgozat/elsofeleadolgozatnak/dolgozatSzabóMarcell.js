// 1. Feladat
function ketTavolsa(x, x1, y, y1){
    Math.pow(x1 - x) * (x1 - x) + (y1 - y) * (y1 - y);
    console.log(-1, 4, 3, 7);
    /*console.log(Math.pow(-1, 4));
    console.log(Math.pow(3, 7));
    return ((x(P) - x(Q)) + (y(P) -y (Q)));*/
}

// 2. Feladat
function szamol() {
    console.log(Math.pow(-1, 4));
    console.log(Math.pow(3, 7));
    return ((x(P)-x(Q)) + (y(P)-y(Q)));
}

// 3. Feladat
function tavolsagok(tavolsag1){
    var tombures = new Array();
    var tomb = new Array[[A, 2, 3],[B, 1, 1],[C, -5, 8]];
    if(tavolsag1 === pontok){
        document.Math.A.value();
        document.Math.B.value();
        document.Math.C.value();
        console.log(tavolsag1);
        console.log("Ez a három pont távolsága.");
    }else if(tavolsag1 % pontok){
        console.log(tavolsag1);
        console.log("Ez a három pont távolsága.");
    }     
}
console.log(tavolsag1);
// 4. Feladat
function leghosszabb(hosz){
    let hosszusag = 10;
    return hosszusag * hosszusag;
}
console.log(hosz);

// 5. Feladat
function tetszolegesPont(pont){
    console.log(pont);
}

